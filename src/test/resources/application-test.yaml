spring:
  config:
    import: optional:${sc.config.dir}/config[.yaml]
  ai:
    model:
      chat: ollama
    ollama:
      init:
        pull-model-strategy: never
sc:
  config:
    dir: ${SC_CONFIG_DIR:file:${user.dir}/.sc}
